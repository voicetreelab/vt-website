
- PR is no longer your feedback loop for green tests.
	- it must be within a hook for agents. Why? 
	- Identify which tests are relevant to modified code -> run those tests -> block agent from stopping if red
- Software engineering is about managing complexity. This is no less true, Now it's more apparent. 
- link to the interesting github
- Modules and coupling just become even more important
- system tests and module tests, unit tests (anything that tests implementation is just debt), unless the unit is highly algorithmically complex.